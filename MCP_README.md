
# 📁 C:\Users\user\Documents\GitHub\Labs - 파일 목록 및 설명

## 📄 텍스트 파일들 (이미 읽음)

| 파일명 | 설명 |
|--------|------|
| `index.html` | 프로젝트의 메인 HTML 파일 |
| `README.md` | 프로젝트 문서화 가이드 |
| `package.json` | npm 의존성 정의 |
| `vite.config.js` | Vite 빌드 설정 |
| `eslint.config.js` | ESLint 규칙 정의 |

## 🖼️ 바이너리/미디어 파일 (open_path 사용 필요)

이 폴더에는 이미지, 영상 같은 **바이너리 파일**이 있을 수 있습니다. 텍스트로 읽을 수 없으므로 반드시 `<open_path>`를 통해 기본 프로그램으로 열어주세요:

- **이미지**: `.png`, `.jpg`, `.gif` 등
- **영상**: `.mp4`, `.mov`, `.avi` 등  
- **아카이브**: `.zip`, `.tar.gz` 등

## 🛠️ Git 관련 파일들

| 파일명 | 설명 |
|--------|------|
| `.git` | Git 저장소 디렉토리 (비어있는 폴더) |
| `.gitignore` | Git에서 무시해야 할 파일 패턴 |

## 📂 하위 폴더 구조

- `backend/` - 백엔드 코드
- `public/` - 퍼블릭 정적 파일
- `server/` - 서버 관련 스크립트 (`spa_server.py`)
- `src/` - 소스 코드

## 🚀 실행 스크립트들

| 파일명 | 설명 |
|--------|------|
| `start.sh` | Linux/Mac 시작 스크립트 |
| `start_server.sh` | 서버 시작용 |
| `stop.sh` | 종료 스크립트 |
| `status.sh` | 현재 상태 확인 |

## 📄 기타 설정 파일

- `CLAUDE.md` - Claude AI 관련 문서
- `GIJO_TOUR_STORYBOARD.txt` - GIJO 투어 스토리보드
- `netlify.toml` - Netlify 배포 설정
- `railway.json` - Railway 클라우드 설정
- `spa_server.py` - SPA 서버용 Python 파일

---

**💡 참고**: Git 커밋이나 원격 저장소 연동은 현재 모델에서 직접 처리 안 되지만, 파일을 수정하고 내용을 읽는 작업은 MCP 로 충분히 가능합니다. 바이너리 파일은 반드시 `<open_path>`로 열어주세요!
